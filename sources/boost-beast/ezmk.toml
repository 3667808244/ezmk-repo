[project]
name = "boost-beast"
type = "static"
version = "1.88.0"
language = "C++17"
header_only = true

[compile]
include_dirs = ["include"]
flags = []

[depends]
lib = ["boost-config", "boost-assert", "boost-core", "boost-asio", "boost-system", "boost-smart-ptr"]
